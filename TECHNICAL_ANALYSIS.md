# PCVRHyFormer 技术解析：从 0.8127 到 0.8311

> 本文是 `README.md` 的技术补充，集中说明 PCVRHyFormer 的主要上分路径、实现机制，以及这些设计为何可能有效。

## 1. 结论先行

| 版本 | 核心变化 | AUC | 单步提升 | 证据强度 |
| --- | --- | ---: | ---: | --- |
| v1 | Baseline | 0.8127 | - | 起点 |
| v3 | Counterfactual Interest Residual (CIR) | 0.8142 | +0.0015 | 顺序迭代结果 |
| v4 | Aligned Pair Tokenizer + Time Dense Features | 0.8150 | +0.0008 | 联合变更 |
| v5 | Fourier Time Encoding + Feature Interaction 等 | 0.8196 | +0.0046 | 联合变更 |
| v6 | 2-layer Gated Deep Feature Interaction | 0.8289 | **+0.0093** | 有反向消融支持 |
| v7 | DIN Target Attention + CIR Gate Update | **0.8311** | +0.0022 | 有近似对照支持 |

最值得关注的结论有三点：

1. **特征交互深度是最强信号。** 两层门控交互带来本路线最大增益；在 v7 上将交互层数退回 1 层，AUC 降至 `0.8184`。
2. **目标感知的历史读取有价值。** CIR 与 DIN 都在回答“当前商品对应哪些历史兴趣”，它们使模型从通用用户表示走向 target-conditioned 表示。
3. **结果存在明显随机波动。** v7 同配置多次运行得到 `0.8311`、`0.8285`、`0.8272`；因此小于约 `0.002` 的单次差异应谨慎解释。

### 如何阅读本文

本文将结论分为三类：

| 标记 | 含义 |
| --- | --- |
| **代码事实** | 可以从版本代码或当前 reference 实现中直接核对 |
| **实验支持** | 有版本对比或回退实验支撑，但可能仍混有随机波动 |
| **机制解释** | 解释为什么设计合理，是可检验假设，不等价于独立消融结论 |

## 2. 任务中的核心困难

pCVR 预测并不是简单判断用户是否喜欢某类商品。模型需要同时处理三类信息：

| 信息类型 | 需要回答的问题 | 建模挑战 |
| --- | --- | --- |
| 非序列特征 | 用户与商品的静态属性如何组合？ | 稀疏、高基数、交互非线性 |
| 多域行为序列 | 用户最近做过什么？ | 多序列长度不同、噪声多 |
| 候选商品条件 | 哪些历史行为与当前商品相关？ | 通用兴趣不等于目标兴趣 |

Baseline 已能编码非序列特征与多域序列，但其主要瓶颈可以概括为：

- 特征之间的组合表达不足；
- 时间信息的表达偏粗；
- 历史兴趣读取不够针对当前候选商品。

后续上分点基本围绕这三个问题展开。

## 3. 最终模型的数据流

v7 / 当前 reference 的主要前向路径可以概括为：

```text
用户离散特征 ─┐
用户连续特征 ─┼─> NS Tokens ─> DeepFeatureInteraction ─┐
商品离散特征 ─┘                                        │
                                                       ├─> QueryGenerator
多域行为序列 ─> Sequence Tokens + Fourier Time Encoding ┘
                         │
                         ├─> HyFormer Blocks ─> Base Output
                         │                          │
候选商品 Tokens ─────────┼─> CIR Delta ────────────┤
                         └─> DIN Delta ────────────┤
                                                    └─> Classifier ─> pCVR
```

最终输出中存在三条加性信息通路：

```python
output = base_output
output = output + sigmoid(cir_gate) * cir_delta
output = output + sigmoid(din_gate) * din_delta
logits = classifier(output)
```

这也是模型有效但仍有扩展空间的地方：主干、CIR 与 DIN 各自提取信息，最终仅在输出层相加，尚未显式协调彼此的注意力或置信度。

## 4. 上分点一：CIR 反事实兴趣残差

### 4.1 改了什么

`CounterfactualInterestResidual` 使用两类 query 读取同一组用户历史：

```text
generic_query：不携带候选商品信息，表示用户的一般兴趣
target_query ：由候选商品 token 生成，表示面向当前商品的兴趣

residual = target_interest - generic_interest
```

代码中的信息融合方式为：

```python
cir_delta = Adapter([
    base_output,
    item_vec,
    target_interest,
    generic_interest,
    target_interest - generic_interest,
])
```

此外，两个读取路径的跨 domain 聚合方式不同：

- `generic_interest` 的 domain gate 只看历史输出；
- `target_interest` 的 domain gate 同时看历史输出、商品向量及其乘积交互。

这意味着候选商品不仅影响序列内注意力，也影响不同历史域的重要性。

### 4.2 为什么可能有效

用户历史中存在大量稳定但不区分目标商品的信号，例如用户整体活跃度、普遍消费意愿、长期类别偏好。这些信号对预测有用，但会淹没“当前商品是否真正触发转化”的细粒度差异。

CIR 的归纳偏置是将二者拆开：

```text
目标相关兴趣 = 用户一般兴趣 + 当前商品额外激活的兴趣
```

因此 `target_interest - generic_interest` 更接近当前商品带来的增量响应。对于 pCVR，这种增量通常比单纯的用户画像更贴近最终转化。

### 4.3 为什么训练较稳

CIR 的最后一层 adapter 为零初始化，因此初始时：

```text
cir_delta = 0
```

新模块不会在训练第一步破坏已有主干表征，后续只有在梯度认为其有价值时才逐渐形成残差。v3-v6 进一步采用 `cir_gate=-3`：

```text
sigmoid(-3) ≈ 0.047
```

这是一种保守接入方式：先让主干形成可用表示，再让额外路径参与。

### 4.4 实验如何支持

| 版本 | CIR 状态 | AUC |
| --- | --- | ---: |
| v1 | 无 CIR | 0.8127 |
| v3 | CIR 进入训练路径，`cir_gate=-3` | 0.8142 |

从 v1 到 v3 累计提升 `+0.0015`，说明 target/generic 分解值得保留。但这一路径并非严格单变量消融，因此更准确的说法是：**CIR 是第一段稳定向上的结构改动**。

## 5. 上分点二：对齐特征 Tokenizer 与时间密集特征

### 5.1 改了什么

v4 同时引入了两项输入侧改动：

| 模块 | 作用 |
| --- | --- |
| `AlignedIntDensePairTokenizer` | 将有对应关系的离散 ID 与连续 value 联合建模 |
| `TimeDenseFeaturesComputer` | 为每个样本补充 58 维时间统计特征 |

对齐 tokenizer 针对 8 组用户字段，将同一字段的 ID 与 value 在 token 内结合，而不是分别进入通用投影：

```text
id embedding + value encoding + field embedding
        -> field-level pooling
        -> learnable query compression
        -> 4 aligned-pair tokens
```

与此同时，已由专门 tokenizer 编码的字段从基础路径中移除，避免同一信号重复输入。

时间密集特征包含：

| 范围 | 示例特征 |
| --- | --- |
| 全局时间 | 小时周期、星期周期、工作时间、周末标记 |
| 单域历史 | 序列长度、距最近行为时间、1h/6h/1d/7d 计数、衰减统计 |
| 跨域聚合 | 活跃域数、总历史长度、跨域近期活跃概括 |

### 5.2 为什么可能有效

**对齐字段不能当作互不相关的普通特征。** 例如一个离散 bucket ID 和对应连续统计值往往描述同一语义实体。若分别投影后再依赖主干自行学习配对，模型会浪费容量，也更容易学到错误交互。

联合 token 化相当于显式告诉模型：

```text
这一组 ID 与 value 应共同解释，而不是任意拼接。
```

**时间统计则补足了状态强度信息。** 序列 token 可以表示“发生了哪些行为”，但用户最近一小时是否密集活跃、最近一次行为有多近，是非常直接的转化先验。这些低维统计量计算便宜，也容易被浅层网络使用。

### 5.3 实验边界

| 版本 | 变更 | AUC |
| --- | --- | ---: |
| v3 | CIR | 0.8142 |
| v4 | + 对齐 tokenizer + 58 维时间特征 | 0.8150 |

提升为 `+0.0008`。由于两项改动同时出现，不能断言其中某一项独立贡献了多少。更合理的结论是：**保持字段结构与增加稳健的近期行为统计，为后续更复杂交互提供了更干净的输入。**

## 6. 上分点三：Fourier 时间编码与首次显式特征交互

### 6.1 改了什么

v5 开始在序列 token 上加入连续的 Fourier 时间编码：

```python
periods = logspace(1 day, 40 days, 12)
time_encoding = Linear([sin(t / periods), cos(t / periods)])
seq_token = seq_token + time_encoding
```

同时，为非序列 token 增加一层乘法交互：

```python
H = expert_proj(X)
X = X + hadamard(X, H)
```

这一版本代码还出现了 `RelativeTimeBias` 等内容。因此 v4 到 v5 的结果应理解为一组升级的联合效果，而不是单个模块的纯增益。

### 6.2 Fourier 时间编码为何合理

已有时间 bucket 主要表达“距离当前有多远”，而绝对时间还包含周期性：

- 一天内的不同时段可能对应不同购买意愿；
- 工作日与周末存在行为模式差异；
- 更长周期上可能存在发薪、活动或复购节奏。

Fourier 编码的优势在于连续且多频率：相邻时间不会因为落入不同桶而突然变成完全无关的向量，模型也可以组合多个周期分量。

### 6.3 乘法交互为何重要

若模型只对静态特征做线性投影，它更擅长学习：

```text
活跃度高 -> 转化更高
价格区间匹配 -> 转化更高
```

而真实推荐决策往往依赖条件组合：

```text
近期活跃度高 且 商品匹配某类偏好 -> 转化显著更高
```

乘法交互 `X * f(X)` 直接提供了表达组合条件的通道，比让后续注意力层间接恢复这些交互更高效。

### 6.4 需要谨慎归因的 RTB

`RelativeTimeBias` 在代码中已经实现，但当前 reference 对默认长度为 `256/512` 的序列保留了 `L <= 128` 才启用的守卫。因此在当前配置下，RTB 不应被作为已验证的主要增益来源。

更稳妥的判断是：

- v5 的 `+0.0046` 支持连续时间表示与初步特征交互这一整体方向；
- RTB 的潜在收益仍需在真正启用它的独立实验中验证。

## 7. 上分点四：两层门控 Deep Feature Interaction

### 7.1 改了什么

v6 将单层无门控交互升级为两层门控残差结构：

```python
for i in range(num_layers):
    H = expert_proj_i(X)
    interaction = einsum("bsd,bsrd->bsd", X, H)
    alpha = sigmoid(layer_gate[i])  # 初始化为 sigmoid(-3) ≈ 0.047
    X = X + alpha * interaction
```

与 v5 相比，变化包括：

| 方面 | v5 | v6 |
| --- | --- | --- |
| 交互层数 | 1 | 2 |
| 门控 | 无显式门控 | 每层可学习 gate |
| 专家投影 | 单层 | 每层独立投影 |

### 7.2 为什么两层比一层更有表达力

将一层交互简化为标量形式：

```text
x' = x + x * f(x)
```

其中 `x * f(x)` 可以表达输入维度之间的二阶组合。第二层接收的输入已经包含二阶项，因此再次交互后，表示中可以出现更高阶组合。

以三个抽象信号为例：

```text
A = 用户近期活跃
B = 商品属性匹配
C = 用户消费能力
```

单层更容易表达 `A*B`、`A*C` 等关系；两层则更容易表达 `(A*B)*C` 这样的条件组合。pCVR 中大量转化信号恰好是这种“多个条件同时成立”的模式。

### 7.3 为什么门控可能改善优化

交互层有表达力，也会放大噪声。若训练开始就让随机初始化的乘法路径全量介入，主干可能先学到不稳定组合。

`gate_init=-3` 使初始交互权重仅约 `4.7%`，意味着模型从接近恒等映射开始训练：

```text
早期：以已有 token 表示为主，交互为弱残差
中后期：若交互确实有用，gate 可被学习放大
```

这可以视为一种可学习的渐进式复杂化机制。需要注意的是，除非保存并分析训练后的 gate 轨迹，否则“第二层一定更晚打开”属于合理解释，而不是已观测事实。

### 7.4 这一点为何最可信

正向演进结果：

| 对比 | AUC 变化 |
| --- | ---: |
| v5 单层交互 -> v6 两层门控交互 | `0.8196 -> 0.8289` (`+0.0093`) |

反向回退结果：

| 对比 | AUC 变化 |
| --- | ---: |
| v7 两层交互 -> v25 训练参数改为一层 | `0.8311 -> 0.8184` (`-0.0127`) |

虽然两次运行都仍受随机性影响，但两个方向均指向同一结论，且幅度远大于已观察到的常规 seed 波动。因此，在所有涨分点中，**两层门控特征交互是证据最强的核心设计**。

## 8. 上分点五：DIN Target Attention 与 CIR 门控调整

### 8.1 DIN 改了什么

DIN 分支直接使用候选商品向量查询原始序列 token：

```python
Q = item_query(item_vec)
K = seq_key(seq_tokens)
weights = softmax(Q @ K / sqrt(d))
pooled_history = sum(weights * seq_tokens)
din_delta = zero_init_adapter(pooled_history)
```

各行为域先独立聚合，再通过可学习 domain 权重合并。DIN 输出同样通过零初始化 adapter 与较小 gate 接入：

```text
din_gate = -3, sigmoid(din_gate) ≈ 0.047
```

### 8.2 DIN 与主干、CIR 的差别

| 路径 | 读取历史时的 query | 擅长表达 |
| --- | --- | --- |
| HyFormer 主干 | 融合后的查询 token | 综合上下文表示 |
| CIR | 目标兴趣减去通用兴趣 | 商品激活的增量兴趣 |
| DIN | 候选商品直接查询历史位置 | 精确 item-history 匹配 |

主干 query 中包含大量用户侧与上下文信息，适合总体判断；DIN 更窄、更直接，适合查找与候选商品高度相关的历史事件。二者互补，而不是简单重复。

### 8.3 CIR gate 为什么同时调整

v7 将：

```text
cir_gate: -3 -> 0
```

即将初始融合强度由约 `4.7%` 提高到 `50%`。它表达的假设是：CIR 已不再只是末端修补，而应更早影响目标相关表示的形成。

### 8.4 实验如何支持，不能说明什么

| 版本 | DIN | CIR gate | AUC |
| --- | --- | ---: | ---: |
| v6 | 无 | -3 | 0.8289 |
| v7 | 有 | 0 | **0.8311** |
| v9 | 有 | -3 | 0.8296 |

可作如下解读：

- v6 到 v7 的 `+0.0022` 表明加入目标直接注意力并强化 CIR 是有效方向；
- v9 与 v7 的差异约 `0.0015`，支持 CIR gate 提高可能有增益；
- v6 与 v9 都处于 `0.829` 附近，说明 DIN 单独贡献可能较小，或者被运行方差遮蔽；
- 由于 v7 的同配置波动范围约为 `0.004`，不能仅依据单次结果精确分解 DIN 与 CIR gate 各自贡献。

换言之，**DIN + CIR 是合理且取得最佳分数的组合，但其细粒度收益仍需要多 seed 消融确认。**

## 9. 哪些尝试没有继续上分

失败或不稳定实验同样能说明模型偏好什么。

| 实验 | AUC | 观察 |
| --- | ---: | --- |
| v10：序列长度扩至 1024 | 0.8216 | 更长历史未必更好，噪声与计算负担可能增加 |
| v12：显式 match side features | 0.8253 | 手工硬匹配没有超过可学习的 target attention |
| v14：dropout 提至 0.05 | 0.8264 | 更强正则未带来提升，但结论受方差影响 |
| v20：DIN match bias | 0.8292 | 接近最终配置中位水平，可能值得多次验证 |
| v23：时间桶调整 | 0.8270 | 时间离散化策略敏感，简单替换可能扰乱已有表示 |
| v25：交互层数由 2 降至 1 | 0.8184 | 强烈支持多层交互的重要性 |

这些结果透露出一个共同趋势：模型更偏好**可学习、软对齐、受门控约束的交互方式**，而非简单增加历史长度或堆入显式规则特征。

## 10. 可扩展方向

基于目前结构，后续实验较有价值的方向包括：

### 10.1 让 CIR 与 DIN 共享信息

当前两条目标感知路径在最终输出处才相加。可以探索：

- DIN 的注意力分布作为 CIR 的 domain/position 先验；
- CIR residual 用于调制 DIN key 或 query；
- 学习一个 gate，在不同样本上动态选择 CIR 与 DIN 的权重。

这比继续平行新增第三条注意力路径更有机会消除功能重复。

### 10.2 针对 DFI 做受控消融

两层交互已被验证最重要，接下来应细化而不是盲目加深：

| 实验 | 目的 |
| --- | --- |
| 固定 2 层，仅扫描 gate 初始化 | 判断优化稳定性是否来自启动强度 |
| 两层共享或不共享 expert | 判断收益来自深度还是参数容量 |
| 分用户/商品/时间 token 的交互 gate | 判断哪些 token 组合真正贡献最大 |

直接增加到三层可能放大过拟合，应以多 seed 结果为准。

### 10.3 重新验证长序列时间建模

RTB 已有实现但在当前默认长度下未成为有效路径。更合适的实验方式是：

- 设计真正支持长序列的局部/分块时间偏置；
- 与不启用 RTB 的同配置进行单变量对比；
- 同时报告速度、显存与 AUC，避免为微小增益付出过大成本。

## 11. 复现实验建议

由于最终结构对随机种子敏感，建议报告格式至少包含：

| 项目 | 建议 |
| --- | --- |
| 运行次数 | 每个关键配置至少 3 次 |
| 指标 | AUC 的 `mean +/- std`，并保留 best |
| 对照方式 | 单次只修改一个模块或关键超参 |
| 配置记录 | 保存 seed、序列截断长度、gate 初始化、交互层数 |

当前结果中应特别保留以下两条判断：

- `0.8311` 是当前观察到的最佳单次 AUC，而非稳定均值；
- 两层门控 DFI 的优势远大于常规波动，是本模型最可靠的结构结论。

## 12. 代码定位

| 内容 | 文件/模块 |
| --- | --- |
| 最终模型主干 | `model.py` / `PCVRHyFormer` |
| CIR | `model.py` / `CounterfactualInterestResidual` |
| DIN | `model.py` / `DINTargetAttention` |
| 时间编码 | `model.py` / `FourierTimeEncoding` |
| 门控交互 | `model.py` / `DeepFeatureInteraction` |
| 对齐特征 tokenizer | `model.py` / `AlignedIntDensePairTokenizer` |
| 58 维时间统计 | `dataset.py` / `TimeDenseFeaturesComputer` |
| 训练入口 | `train.py` 与 `run.sh` |

总体来看，PCVRHyFormer 的上分主线并非简单扩大模型规模，而是逐步加入三个更贴近 pCVR 本质的归纳偏置：**结构化字段表示、可控的高阶交互、候选商品条件下的历史兴趣读取**。其中两层门控特征交互已表现出最稳定的价值，CIR 与 DIN 则提供了继续优化目标感知建模的入口。
